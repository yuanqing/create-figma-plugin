{
  "private": true,
  "scripts": {
    "build": "lerna run build",
    "bump-dependencies": "ts-node scripts/bump-dependencies.ts",
    "clean": "lerna run --parallel clean && lerna run --scope @create-figma-plugin/ui storybook-clean && yarn run website-clean && rm -rf '*.log'",
    "docs": "lerna run --scope @create-figma-plugin/utilities docs",
    "fix": "lerna run --parallel fix && eslint --fix 'scripts/**/*.ts' && prettier --ignore-path lerna.json --loglevel error --write '*.json'",
    "generate-index-ts": "lerna run generate-index-ts --parallel --stream",
    "lint": "lerna run --parallel lint && eslint 'scripts/**/*.ts'",
    "postinstall": "simple-git-hooks && yarn run build",
    "publish": "yarn run build && lerna publish from-package",
    "reset": "yarn run clean && lerna clean --yes && rm -rf node_modules yarn.lock && yarn install",
    "test": "lerna run test",
    "version": "lerna version",
    "website-build": "yarn run website-clean && yarn run website-build-docs && yarn run website-build-storybook",
    "website-build-docs": "yarn run docs && single-page-markdown-website docs/index.md",
    "website-build-storybook": "lerna run --scope @create-figma-plugin/ui storybook-build && cpy packages/ui/build build/ui",
    "website-clean": "rm -rf build",
    "website-deploy": "yarn run website-build && gh-pages --dist build"
  },
  "devDependencies": {
    "@types/node": "^14.14.37",
    "cpy-cli": "^3.1.1",
    "eslint": "^7.24.0",
    "eslint-config-yuanqing": "^0.0.4",
    "gh-pages": "^3.1.0",
    "globby": "^11.0.3",
    "lerna": "^4.0.0",
    "lint-staged": "^10.5.4",
    "npm-check-updates": "^11.4.1",
    "prettier": "^2.2.1",
    "simple-git-hooks": "^2.3.1",
    "single-page-markdown-website": "^0.0.14",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "eslintConfig": {
    "extends": "yuanqing",
    "ignorePatterns": [
      "*.css.d.ts",
      "packages/create-figma-plugin/plugin-templates"
    ],
    "rules": {
      "@typescript-eslint/explicit-module-boundary-types": [
        0
      ],
      "react/no-unknown-property": [
        2,
        {
          "ignore": [
            "class",
            "clip-rule",
            "fill-rule"
          ]
        }
      ],
      "react/prop-types": [
        "off"
      ]
    }
  },
  "lint-staged": {
    "*.{js,ts,tsx}": [
      "eslint"
    ],
    "*.css": [
      "stylelint",
      "prettier"
    ]
  },
  "prettier": "eslint-config-yuanqing/prettier",
  "simple-git-hooks": {
    "pre-commit": "yarn run lint-staged"
  },
  "single-page-markdown-website": {
    "title": "Create Figma Plugin",
    "description": "A comprehensive toolkit for developing Figma plugins",
    "links": [
      {
        "text": "Storybook",
        "url": "https://yuanqing.github.io/create-figma-plugin/ui/"
      },
      {
        "text": "GitHub",
        "url": "https://github.com/yuanqing/create-figma-plugin"
      }
    ]
  },
  "stylelint": {
    "extends": "stylelint-config-yuanqing"
  },
  "workspaces": [
    "packages/*"
  ]
}
