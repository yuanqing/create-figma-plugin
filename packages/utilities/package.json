{
  "name": "@create-figma-plugin/utilities",
  "version": "0.0.85-alpha.4",
  "publishConfig": {
    "access": "public"
  },
  "description": "Helpful utility functions for common Figma plugin operations",
  "keywords": [
    "create-figma-plugin",
    "figma",
    "figma-plugin",
    "figma-plugins"
  ],
  "license": "MIT",
  "author": "Yuan Qing Lim",
  "repository": {
    "type": "git",
    "url": "git://github.com/yuanqing/create-figma-plugin.git",
    "directory": "packages/utilities"
  },
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "scripts": {
    "build": "yarn run clean-build && tsc --module ES2020 --moduleResolution Node",
    "clean": "yarn run clean-build && yarn run clean-test",
    "clean-build": "rm -rf '*.log' lib tsconfig.tsbuildinfo",
    "clean-test": "rm -rf '*.log' .nyc_output coverage",
    "docs": "ts-node scripts/generate-docs.ts",
    "fix": "eslint --fix '{scripts,src,test}/**/*.ts' && prettier --loglevel error --write '*.json'",
    "lint": "eslint '{scripts,src,test}/**/*.ts'",
    "test": "tsc -b && yarn run clean-test && tap 'test/**/*.ts' --coverage-report html --coverage-report text --no-browser --no-check-coverage --reporter terse --timeout 120 --ts"
  },
  "dependencies": {
    "natural-compare-lite": "1.4.0",
    "type-fest": "^1.0.2"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.19.3",
    "@types/fs-extra": "^9.0.10",
    "@types/natural-compare-lite": "^1.4.0",
    "@types/node": "^14.14.37",
    "@types/tap": "^14.10.3",
    "eslint": "^7.24.0",
    "eslint-config-yuanqing": "^0.0.4",
    "fs-extra": "^9.1.0",
    "generate-ts-docs": "^0.0.6",
    "prettier": "^2.2.1",
    "tap": "^15.0.2",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.4"
  },
  "sideEffects": false
}
